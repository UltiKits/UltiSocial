# UltiSocial — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only,
  as `.neg-<slug>`.
- **Layer**, copied verbatim from Laojun's own `ultitools-real-client-uat` skill: `protocol`,
  `java-client`, `os-input`, `pixel`, `server`, `human`.
- **Human-authenticated-session rows (D-27b):** this module has no panel-facing surface at all,
  so no row below is affected; the convention is stated here for template consistency.
- **Expected** must name an observable truth and never the words "it works". Every chat line, GUI
  text and console line this module writes follows the framework's `language` setting
  (`UltiKits/UltiSocial#14`). Every row whose Expected quotes a line therefore carries the
  precondition `language: en` in `plugins/UltiTools/config.yml` and quotes the English text exactly
  (colour codes rendered). The friend-list title and the ten `messages.*` settings in
  `config/social.yml` are written into that file in the server's language while they are still
  built-in text, and the module shows the file's text; rows that quote them assume the file holds
  the English text this build writes under `language: en` (any start of this build under
  `language: en` leaves it so, unless a value was edited by hand). Before the session, `plugins/UltiTools/pluginConfig/UltiSocial/lang/`
  is moved aside so this jar's catalogues are extracted fresh (an upgraded install never refreshes an
  already-extracted language file, `UltiKits/UltiTools-Reborn#459`; `ultisocial.i18n.language`).
- **Covers** back-references a Phase 9 GUI-excluded class name; left blank when no such class
  applies. This module owns both of its GUI-excluded classes (`FriendListGUI`, `BlockListGUI`) —
  each is named in exactly one row's Covers cell below.
- A row whose Preconditions name a prior row must appear after that row in file order — asserted
  mechanically: for every row, every checklist ID cited in its Preconditions cell must have a
  strictly smaller line number in this file (sweep class 8, D-27a).
- **Config-per-file rule (D-06):** one checklist row per `@ConfigEntity`-annotated class or per
  shipped yml file. This module has exactly one such file (`config/social.yml`, generated from
  `SocialConfig`'s `@ConfigEntry` defaults — no packaged seed resource exists), so exactly one
  config-per-file row appears below (`ultisocial.config.social-yml`).
- **Config text rows:** `ultisocial.config.social.materialize-fresh`, `ultisocial.config.social.materialize-switch`
  and `ultisocial.config.social.materialize-upgrade` exercise `FEATURES.md`'s
  `ultisocial.lifecycle.legacy-text-defaults` on a fresh file, across a `language` switch and on a file an earlier
  version wrote; they are named by the phase's config-text convention (`<prefix>.config.<file stem>.materialize-*`)
  rather than by that ID, and the row `ultisocial.lifecycle.legacy-text-defaults` itself covers the same step on a
  single-module `ul reload UltiSocial`. They sit under `## Lifecycle Hooks`, not `## Configuration`, which holds one
  row per config file.
- **Nearly every row below needs a second player** (this is a friend/block system — almost
  nothing in it operates on a single account), named explicitly in Preconditions rather than
  left implied.

## Friend Management Commands

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultisocial.friend.add | `language: en` in the framework's `config.yml`; `Tester1` and `Tester2` online, not already friends, not blocked in either direction, neither at `max_friends` (shipped default 50) | `Tester1` runs `/friend add Tester2` | `Tester1` receives `Friend request sent to Tester2!` (`messages.request_sent`, the file's text); `Tester2` receives `Tester1 wants to be your friend! Type /friend accept Tester1 to accept` (`messages.request_received`) | server | |
| ultisocial.friend.add.neg-blocked | `language: en` in the framework's `config.yml`; `Tester1` and `Tester3` online; `Tester1` has blocked `Tester3` — run `/friend block Tester3` as `Tester1` to reach this state before attempting this row (the checklist row exercising `/friend block` itself appears later in this file; blocking is mechanically simple enough not to need its own row run first) | `Tester3` runs `/friend add Tester1` | `Tester3` receives `Cannot perform friend operations with Tester1 due to blacklist` (`messages.blocked`); no pending request is created | server | |
| ultisocial.friend.add.neg-already-friends | `language: en` in the framework's `config.yml`; `Tester1` and `Tester2` are already friends — run `/friend add Tester2` as `Tester1`, then `/friend accept Tester1` as `Tester2`, to reach this state before attempting this row (the checklist row exercising `/friend accept` itself, with its own full assertions, appears later in this file) | `Tester1` runs `/friend add Tester2` again | `Tester1` receives `You are already friends with Tester2!` (`messages.already_friends`); no duplicate `FriendshipData` row is created | server | |
| ultisocial.friend.add.neg-self | `language: en` in the framework's `config.yml`; `Tester1` online | `Tester1` runs `/friend add Tester1` | `Tester1` receives `You cannot add yourself as a friend!` | server | |
| ultisocial.friend.add.neg-offline | `language: en` in the framework's `config.yml`; `Tester1` online; no player named `NotOnline` currently online | `Tester1` runs `/friend add NotOnline` | `Tester1` receives `Player NotOnline is not online!` | server | |
| ultisocial.friend.requests | `language: en` in the framework's `config.yml`; `Tester2` has a pending request from `Tester1` (`ultisocial.friend.add`, run first) | `Tester2` runs `/friend requests` | Chat shows `=== Friend Requests ===`, then exactly one line `- Tester1 (to accept: /friend accept Tester1)` | server | |
| ultisocial.friend.requests.neg-empty | `language: en` in the framework's `config.yml`; `Tester3` has zero pending requests | `Tester3` runs `/friend requests` | Chat shows `You have no pending friend requests` and no header or list lines | server | |
| ultisocial.friend.accept | `language: en` in the framework's `config.yml`; `Tester2` has a pending request from `Tester1` (`ultisocial.friend.requests`, run first); neither is currently at `max_friends` | `Tester2` runs `/friend accept Tester1` | `Tester2` receives `You and Tester1 are now friends!` and `Tester1` receives `You and Tester2 are now friends!` (`messages.friend_added`, sent to each side naming the other); two `FriendshipData` rows now exist (one per direction); the pending request is gone from `Tester2`'s `/friend requests` | server | |
| ultisocial.friend.accept.neg-no-pending | `language: en` in the framework's `config.yml`; `Tester3` has zero pending friend requests from anyone (check with `/friend requests`; a pending request from any other player gives a different reply) | `Tester3` runs `/friend accept Tester1` | `Tester3` receives the i18n `no_pending_request` message, English text under `language: en`: `No pending friend request from Tester1!` | server | |
| ultisocial.friend.accept.neg-mutual-auto-accept | `language: en` in the framework's `config.yml`; `Tester1` and `Tester3` online, not already friends, not blocked; `Tester3` has already sent `Tester1` a pending request (run `/friend add Tester1` as `Tester3` first) | `Tester1` runs `/friend add Tester3` (NOT `/friend accept`) | `sendRequest`'s own mutual-request branch fires: the two become friends immediately (both receive the `messages.friend_added` text, `You and <the other> are now friends!`) WITHOUT `Tester1` ever running `/friend accept` — `Tester3`'s original pending request is consumed, not left in the queue alongside a fresh reverse one | server | |
| ultisocial.friend.list | `language: en` in the framework's `config.yml`; `Tester1` has exactly one friend, `Tester2` (from `ultisocial.friend.accept`) | `Tester1` runs `/friend list` | Chat shows `=== Friends (1) ===` (count matches), then one line `● Online Tester2` with a green indicator (since `Tester2` is online) | server | |
| ultisocial.friend.list.neg-empty | `language: en` in the framework's `config.yml`; `Tester3` has zero friends | `Tester3` runs `/friend list` | Chat shows `You have no friends yet. Use /friend add <player> to add one`, and no header or list lines | server | |
| ultisocial.friend.open | `Tester1` holds `ultisocial.use`, with zero or more existing friends | Run bare `/friend` (no arguments) | The `ultisocial.gui.friend-list` GUI opens for `Tester1`; this row proves only that the bare command opens the browser — its own interactions are covered by `ultisocial.gui.friend-list` below | server | FriendListGUI |
| ultisocial.friend.deny | `language: en` in the framework's `config.yml`; `Tester1` has a pending request from `Tester3` (run `/friend add Tester1` as `Tester3` first, with `Tester1` and `Tester3` NOT already friends and NOT blocked) | `Tester1` runs `/friend deny Tester3` | `Tester1` receives `You denied Tester3's friend request` (`messages.request_denied`); the request is gone from `Tester1`'s `/friend requests`; `Tester1` and `Tester3` are NOT friends afterward | server | |
| ultisocial.friend.deny.neg-no-pending | `language: en` in the framework's `config.yml`; `Tester2` has zero pending friend requests from anyone (check with `/friend requests`; a pending request from any other player gives a different reply) | `Tester2` runs `/friend deny Tester3` | `Tester2` receives the i18n `no_pending_request` message, English text under `language: en`: `No pending friend request from Tester3!` | server | |
| ultisocial.friend.remove | `language: en` in the framework's `config.yml`; `Tester1` and `Tester2` are friends (from `ultisocial.friend.accept`) | `Tester1` runs `/friend remove Tester2` | `Tester1` receives `You have removed Tester2 from your friends` (`messages.friend_removed`, sent ONLY to `Tester1`, not to `Tester2`); BOTH `FriendshipData` rows (both directions) are gone from the database; `Tester2`'s own `/friend list` no longer shows `Tester1` either | server | |
| ultisocial.friend.remove.neg-not-friend | `language: en` in the framework's `config.yml`; `Tester1` and `Tester3` are not friends | `Tester1` runs `/friend remove Tester3` | `Tester1` receives the i18n `not_friend` message, English text under `language: en`: `Tester3 is not your friend!` | server | |
| ultisocial.friend.tp | `language: en` in the framework's `config.yml`; `Tester1` and `Tester2` are friends, `Tester2` online in a different location; `tp_to_friend.enabled: true` (shipped default); `Tester1` not on cooldown | `Tester1` runs `/friend tp Tester2` | `Tester1` is teleported to `Tester2`'s location; `Tester1` receives `Teleported to Tester2!` | server | |
| ultisocial.friend.tp.neg-disabled | `language: en` in the framework's `config.yml`; Same friendship as above; `tp_to_friend.enabled: false` (NOT the shipped default) | `Tester1` runs `/friend tp Tester2` | `Tester1` receives `Teleport to friend feature is disabled!`; `Tester1`'s location is unchanged | server | |
| ultisocial.friend.tp.neg-cooldown | `language: en` in the framework's `config.yml`; Same friendship as above, `tp_to_friend.enabled: true`; `Tester1` just successfully teleported via `ultisocial.friend.tp` within the last `tp_to_friend.cooldown` seconds (shipped default 30) | `Tester1` runs `/friend tp Tester2` again immediately | `Tester1` receives `Teleport on cooldown! Please wait N seconds` with N a positive count; `Tester1`'s location is unchanged | server | |
| ultisocial.friend.msg | `language: en` in the framework's `config.yml`; `Tester1` and `Tester2` are friends, both online | `Tester1` runs `/friend msg Tester2 hello there`| `Tester2` receives `[PM] Tester1 → you: hello there`; `Tester1` receives `[PM] you → Tester2: hello there` (the message text exactly as typed) | server | |
| ultisocial.friend.msg.neg-not-friend | `language: en` in the framework's `config.yml`; `Tester1` and `Tester3` are not friends | `Tester1` runs `/friend msg Tester3 hi` | `Tester1` receives `Tester3 is not your friend! You can only send private messages to friends`; `Tester3` receives nothing | server | |
| ultisocial.friend.msg.neg-empty | `language: en` in the framework's `config.yml`; `Tester1` and `Tester2` are friends, both online | `Tester1` runs `/friend msg Tester2 ` followed by only whitespace characters | `Tester1` receives `Enter a message to send! Usage: /friend msg <friend> <message>`; `Tester2` receives nothing | server | |
| ultisocial.friend.block | `language: en` in the framework's `config.yml`; `Tester1` and `Tester3` online, not already blocked in either direction | `Tester1` runs `/friend block Tester3` | `Tester1` receives `You have blocked Tester3` (the language file's `player_blocked`; the configuration key `messages.player_blocked`, which nothing ever read, is removed — `UltiKits/UltiSocial#23`); a new `BlacklistData` row now exists for `Tester1`→`Tester3` | server | |
| ultisocial.friend.block.neg-self | `language: en` in the framework's `config.yml`; `Tester1` online | `Tester1` runs `/friend block Tester1` | `Tester1` receives `You cannot block yourself!`; no `BlacklistData` row is created | server | |
| ultisocial.friend.block.neg-already-blocked | `language: en` in the framework's `config.yml`; `Tester1` has already blocked `Tester3` (from `ultisocial.friend.block`) | `Tester1` runs `/friend block Tester3` again | `Tester1` receives `Tester3 is already on your blacklist!`; no duplicate `BlacklistData` row is created | server | |
| ultisocial.friend.block.neg-unfriends | `language: en` in the framework's `config.yml`; `Tester1` and `Tester2` are friends (from `ultisocial.friend.accept`, re-established if `ultisocial.friend.remove` already ran against this pair — use a fresh friendship for this row) | `Tester1` runs `/friend block Tester2` | `Tester1` receives ONLY `You have blocked Tester2` — the additional line `(Friend relationship has been automatically removed)` is dead code and never sends: `FriendService#addToBlacklist` already calls `removeFriendByUuid` (clearing both `FriendshipData` rows AND both friend caches) BEFORE `FriendCommand#blockPlayer`'s own `areFriends` check runs, so that check always evaluates false by the time it executes. Known product defect, `UltiKits/UltiSocial#17`, not fixed here per this phase's zero-new-code rule. Both `FriendshipData` rows for this pair ARE still gone (the removal itself works correctly — only the confirmatory chat line never fires), matching `ultisocial.friend.remove`'s own two-sided deletion | server | |
| ultisocial.friend.unblock | `language: en` in the framework's `config.yml`; `Tester1` has blocked `Tester3` (from `ultisocial.friend.block`) | `Tester1` runs `/friend unblock Tester3` | `Tester1` receives `You have unblocked Tester3` (the language file's `player_unblocked`; `messages.player_unblocked`, which nothing ever read, is removed — `UltiKits/UltiSocial#23`); the `BlacklistData` row for `Tester1`→`Tester3` is gone; `Tester3` can now send `Tester1` a friend request again | server | |
| ultisocial.friend.unblock.neg-not-blocked | `language: en` in the framework's `config.yml`; `Tester1` has not blocked `Tester2` | `Tester1` runs `/friend unblock Tester2` | `Tester1` receives `Tester2 is not on your blacklist!` | server | |
| ultisocial.friend.blocklist | `Tester1` has at least one blocked player (from `ultisocial.friend.block`, re-block `Tester3` if `ultisocial.friend.unblock` already ran) | `Tester1` runs `/friend blocklist` | The `ultisocial.gui.block-list` GUI opens for `Tester1`; this row proves only that the command opens the browser — its own interactions are covered by `ultisocial.gui.block-list` below | server | BlockListGUI |
| ultisocial.friend.help | `language: en` in the framework's `config.yml`; `Tester1` online | `Tester1` runs `/friend help` | Chat shows these fourteen lines in order: `=== Friend System Help ===`, `/friend - Open friend list`, `/friend list - List all friends`, `/friend add <player> - Send friend request`, `/friend accept <player> - Accept friend request`, `/friend deny <player> - Deny friend request`, `/friend remove <player> - Remove friend`, `/friend tp <friend> - Teleport to friend`, `/friend msg <friend> <message> - Private message friend`, `/friend requests - View pending requests`, `=== Blacklist Features ===`, `/friend block <player> - Block player`, `/friend unblock <player> - Unblock player`, `/friend blocklist - View blacklist`; none contains Chinese text | server | |

## GUI

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultisocial.gui.friend-list | `language: en` in the framework's `config.yml`; `LaoJunClient` (the pixel-layer test player) has at least 2 friends, one online (`Tester2`) and one offline; `tp_to_friend.enabled: true` (shipped default); `LaoJunClient` NOT currently on teleport cooldown (has not run `ultisocial.friend.tp` within the last `tp_to_friend.cooldown` seconds, or is a fresh session); `LaoJunClient` runs bare `/friend` (not `Tester1` — `ultisocial.friend.open`'s own row uses `Tester1`, a different account) to open the browser for itself | Left-click the online friend's item; separately (re-open), shift+left-click the same item; separately, right-click the online friend's item; separately, right-click the offline friend's item; separately, shift+right-click any friend item; separately, click the pending-requests button (slot 47) when `LaoJunClient` has at least one pending request | Left-click on an online friend teleports exactly as `ultisocial.friend.tp` does; shift+left-click toggles favorite status (chat shows `Favorite status updated!`, and the item's display name gains/loses its star marker on the next `refresh()`); right-click on an online friend closes the inventory and prints `Use the command to send a private message: /friend msg <name> <message>` (with the friend's name) rather than sending anything; right-click on an OFFLINE friend instead removes them immediately (same effect as `ultisocial.friend.remove`, no confirmation dialogue); shift+right-click always removes regardless of online state; clicking the pending-requests button closes the inventory and runs `/friend requests` as if typed | pixel | FriendListGUI |
| ultisocial.gui.friend-list.neg-no-pending-button | `LaoJunClient` (the pixel-layer test player) has zero pending requests; `LaoJunClient` runs bare `/friend` (not `Tester1`) to open the browser for itself | Observe slot 47 | Slot 47 holds the plain gray-glass-pane filler item (the SAME filler `addNavigationRow` places across all of slots 45-53 before conditionally overwriting 47), NOT the yellow writable-book pending-requests button — `addNavigationRow` fills the whole bottom row with filler first and only replaces slot 47 with the button when `getPendingRequests(...).size() > 0`, so slot 47 is never literally empty, only ever one of these two specific items | pixel | FriendListGUI |
| ultisocial.gui.block-list | `LaoJunClient` (the pixel-layer test player) has at least 2 blocked players; `LaoJunClient` runs `/friend blocklist` (not `Tester1` — `ultisocial.friend.blocklist`'s own row uses `Tester1`, a different account) to open the browser for itself | Left-click a blocked-player item; separately, click the Back button (slot 47) | Left-click unblocks that player exactly as `ultisocial.friend.unblock` does, and the GUI refreshes with one fewer entry; the Back button closes this inventory and opens `ultisocial.gui.friend-list` for the same viewer | pixel | BlockListGUI |
| ultisocial.gui.block-list.neg-empty-state | `language: en` in the framework's `config.yml`; `LaoJunClient` (the pixel-layer test player) has zero blocked players; `LaoJunClient` runs `/friend blocklist` (not `Tester1`) to open the browser for itself | Observe slot 22 | An item named `Your blacklist is empty`, with the lore line `You have not blocked anyone`, appears at slot 22 — this item appears ONLY when the blacklist is empty, and disappears once at least one entry exists | pixel | BlockListGUI |

## Player Presence Notifications

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultisocial.event.friend-online-notify | `language: en` in the framework's `config.yml`; `notifications.friend_online: true` (shipped default); `Tester1` and `Tester2` are friends; `Tester1` currently offline, `Tester2` online | `Tester1` joins the server | `Tester2` receives `Your friend Tester1 is now online!` (`messages.friend_online`) | server | |
| ultisocial.event.friend-online-notify.neg-disabled | `notifications.friend_online: false` (NOT the shipped default); same friendship as above | `Tester1` joins the server | `Tester2` receives no notification | server | |
| ultisocial.event.friend-offline-notify | `language: en` in the framework's `config.yml`; `notifications.friend_offline: true` (shipped default); `Tester1` and `Tester2` are friends, both online | `Tester1` disconnects (quit, not kick) | `Tester2` receives `Your friend Tester1 has gone offline` (`messages.friend_offline`); `Tester1`'s friend/blacklist caches are cleared regardless of this toggle | server | |
| ultisocial.event.friend-offline-notify.neg-disabled | `notifications.friend_offline: false` (NOT the shipped default); same friendship as above, both online | `Tester1` disconnects | `Tester2` receives no notification; `Tester1`'s caches are still cleared (the cache-clear is unconditional, independent of this toggle) | server | |

## Lifecycle Hooks

`/ul reload <name>` calls that module's `reloadSelf()`, which re-reads the module's config files
into the running beans (as of UltiTools 6.3.0 a module can no longer replace this step), then logs
the framework's own line `Module '<name>' reloaded.`; the command then replies `Module <name> has been reloaded` to its
sender. `<name>` is the module's runtime name, the `name:` key of its own `plugin.yml` (`UltiSocial`).
Both lines are framework strings localised by the framework's `language` setting, and so is this
module's own text in the row, hence the row's `language: en` precondition. `/ul` requires op, so the row runs it from the server console.
`ultisocial.lifecycle.reload` supersedes `ultisocial.event.module-reload` (retired with
`UltiKits/UltiSocial#13`; its Phase 10 verdict recorded the defect, not this behaviour).
`ultisocial.lifecycle.removed-key-warning`'s WARNING comes from this module's language file, so it
carries the `language: en` precondition too.
`ultisocial.lifecycle.legacy-text-defaults` exercises the step that rewrites a title or message still
holding built-in text in the server's language, on a single-module `ul reload UltiSocial`.
`ultisocial.config.social.materialize-fresh` and `ultisocial.config.social.materialize-switch` exercise the
same step on a file written fresh and across a `language` switch in both directions; they run back to
back, the second continuing from the first's state and restoring the first's backups.
`ultisocial.config.social.materialize-upgrade` exercises it at start-up on a file an earlier version wrote.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultisocial.lifecycle.reload | `language: en` in `plugins/UltiTools/config.yml`; `UltiSocial` loaded; `tp_to_friend.enabled: true` (the shipped default) in `plugins/UltiTools/pluginConfig/UltiSocial/config/social.yml`; `Tester1` online and holding `ultisocial.use`; no player named `NoSuchFriend01` is on `Tester1`'s friends list (`/friend list` does not show it) | 1. As `Tester1`, run `/friend tp NoSuchFriend01`. 2. Edit `tp_to_friend.enabled` to `false` in that `social.yml` and save the file; from the server console, on the SAME running server (no restart), run `ul reload UltiSocial`. 3. As `Tester1`, run `/friend tp NoSuchFriend01` again. 4. Cleanup: set `tp_to_friend.enabled` back to `true`, save, and run `ul reload UltiSocial` from the console. | Step 1: `Tester1` receives `NoSuchFriend01 is not your friend!` (red) — this proves the teleport feature is enabled before the edit, so step 3 can discriminate; if `Tester1` instead receives `Teleport to friend feature is disabled!`, the preconditions are wrong: record the row `blocked`, never `pass`. Step 2: the console shows `Module 'UltiSocial' reloaded.` and the reply `Module UltiSocial has been reloaded`, and no `UltiSocial configuration reloaded!` line (on a server whose `social.yml` still holds `notifications.friend_join_world`, a `[UltiSocial]` WARNING naming that key also appears; that line is `ultisocial.lifecycle.removed-key-warning`'s, and this row neither requires nor forbids it). Step 3: `Tester1` receives `Teleport to friend feature is disabled!` (red, the same line as `ultisocial.friend.tp.neg-disabled`) and NOT the `is not your friend!` line — the edited `tp_to_friend.enabled` took effect without a restart. On the pre-migration module the configuration was never re-read on `/ul reload`, so step 3 would repeat step 1's sentence | server | |
| ultisocial.lifecycle.removed-key-warning | `language: en` in the framework's `config.yml`; `UltiSocial` loaded from this build; the server console readable; `plugins/UltiTools/pluginConfig/UltiSocial/config/social.yml` contains the keys `notifications.friend_join_world`, `messages.player_blocked` and `messages.player_unblocked` with any values — any server that ran an earlier version has them, because the framework wrote them there and never deletes a key; if one is absent, stop the server, add it (`  friend_join_world: false` under the file's `notifications:` block at the same indentation as its `friend_online` line; `  player_blocked: x` and `  player_unblocked: x` under its `messages:` block), and save | 1. Start the server (or restart it) and read the console output of UltiSocial's load. 2. From the console run `ul reload UltiSocial` and read the output. 3. Delete the `friend_join_world`, `player_blocked` and `player_unblocked` lines from that `social.yml`, change nothing else, save, and run `ul reload UltiSocial` again. 4. Cleanup: none — the keys do nothing in this version, so they stay deleted | Steps 1 and 2 each: exactly three WARNING lines from `[UltiSocial]`, each containing the absolute path of that `social.yml` (ending in `pluginConfig/UltiSocial/config/social.yml`) and ending `Delete the key from the file to silence this warning.`: one containing `still contains 'notifications.friend_join_world', which this version of UltiSocial no longer reads` and `UltiKits/UltiSocial#21`; one each containing `still contains 'messages.player_blocked'` and `still contains 'messages.player_unblocked'`, both naming the language file and `UltiKits/UltiSocial#23`. Step 3: after the keys are deleted, the reload prints no such line — steps 1 and 2 are the positive control that the check can see the keys, and step 3 shows the keys are what drive it (UltiKits/UltiSocial#15, UltiKits/UltiSocial#23) | server | |
| ultisocial.lifecycle.legacy-text-defaults | `language: en` in the framework's `config.yml`; the server running this build; `plugins/UltiTools/pluginConfig/UltiSocial/config/social.yml` holding the English text this build writes (any start of this build under `language: en` leaves it so); a byte copy of that file saved outside the server with its SHA-256; `notifications.friend_online` and `notifications.friend_offline` true; `Tester1` and `Tester2` are friends, `Tester1` offline and `Tester2` online | 1. On the running server, edit that `social.yml`: set `messages.friend_online` to exactly `&a你的好友 {PLAYER} 上线了！` (the default every earlier version shipped) and `messages.friend_offline` to `&7Bye {PLAYER}` (an operator's own value); save. 2. From the console run `ul reload UltiSocial`, then read the two keys in the file. 3. `Tester1` joins, then disconnects; read `Tester2`'s chat. 4. Cleanup: stop the server and restore the byte copy (SHA-256 read back) | Step 2: `messages.friend_online` is now `&aYour friend {PLAYER} is now online!` — the old default was rewritten in the server's language by the module's reload and the file saved — and `messages.friend_offline` is still `&7Bye {PLAYER}`. Step 3: `Tester2` receives `Your friend Tester1 is now online!` (green) — exactly the file's text — and then `Bye Tester1` (grey) | server | |
| ultisocial.config.social.materialize-fresh | `language: en` in the framework's `config.yml`; the server is stopped; `plugins/UltiTools/pluginConfig/UltiSocial/config/social.yml` and `plugins/UltiTools/pluginConfig/UltiSocial/lang/` moved to a backup directory outside the server (SHA-256 of each recorded), so both are written fresh; `Tester1` and `Tester2` hold `ultisocial.use`, are not friends, are not blocked in either direction, and have no pending request between them | 1. Start the server. 2. Read `gui_title` and the ten `messages.*` keys in `plugins/UltiTools/pluginConfig/UltiSocial/config/social.yml`. 3. With `Tester1` and `Tester2` online, `Tester1` runs `/friend add Tester2`; read both players' chat. 4. Undo the request: `Tester2` runs `/friend deny Tester1`; read `Tester2`'s chat | Step 2: read as YAML, `gui_title` is `&6Friend List &7({COUNT}/{MAX})`, `messages.request_sent` is `&aFriend request sent to {PLAYER}!`, `messages.request_received` is `&e{PLAYER} wants to be your friend! Type /friend accept {PLAYER} to accept`, `messages.request_denied` is `&cYou denied {PLAYER}'s friend request`, and the other seven are in English too — no Chinese text in any of the eleven. Step 3: `Tester1` receives `Friend request sent to Tester2!` (green) and `Tester2` receives `Tester1 wants to be your friend! Type /friend accept Tester1 to accept` (yellow) — exactly the file's text with the name substituted. Step 4: `Tester2` receives `You denied Tester1's friend request` (red), and `Tester2`'s `/friend requests` no longer lists `Tester1`. Leave the server running and the file as written: `ultisocial.config.social.materialize-switch` continues from this state | server | |
| ultisocial.config.social.materialize-switch | `ultisocial.config.social.materialize-fresh` passed and `plugins/UltiTools/pluginConfig/UltiSocial/config/social.yml` still holds the English text it wrote, unedited, except `messages.request_denied` set by hand to `&cNo thanks, {PLAYER}` and saved; the server is still running with `language: en` (run this row straight after `ultisocial.config.social.materialize-fresh`, before its backups are restored); `plugins/UltiTools/config.yml` backed up with its SHA-256 | 1. Set `language: zh` in `plugins/UltiTools/config.yml`, save, and run bare `ul reload` from the console (not `ul reload UltiSocial`, which does not re-read `language`). 2. Read `messages.request_sent` and `messages.request_denied`. 3. Set `language: en`, save, run bare `ul reload` again, and read the two keys. 4. Cleanup: stop the server; restore `plugins/UltiTools/config.yml` from its backup and `config/social.yml` and `lang/` from the backups `ultisocial.config.social.materialize-fresh` took (SHA-256 of each read back) | Step 2: `messages.request_sent` is byte-identical to `request_sent` of the jar's `lang/zh.yml` (the Chinese text earlier versions shipped), and `messages.request_denied` is still `&cNo thanks, {PLAYER}`. Step 3: `messages.request_sent` is `&aFriend request sent to {PLAYER}!` again and `messages.request_denied` is still `&cNo thanks, {PLAYER}` — an untouched value follows the switch in both directions, an edited one never does | server | |
| ultisocial.config.social.materialize-upgrade | `language: en` in the framework's `config.yml`; server stopped; `plugins/UltiTools/pluginConfig/UltiSocial/config/social.yml` as an earlier version wrote it: `gui_title` and every `messages.*` value at the Chinese default every earlier version shipped (the shared server's state), except `messages.friend_offline`, set to `&7Bye {PLAYER}` (an operator's own value); a byte copy of the file saved outside the server with its SHA-256; `notifications.friend_online` and `notifications.friend_offline` true; `Tester1` and `Tester2` are friends, `Tester1` offline and `Tester2` online | 1. Start the server, record the SHA-256 of `config/social.yml`, and read `gui_title`, `messages.friend_online`, `messages.request_sent` and `messages.friend_offline` in it. 2. `Tester1` joins, then disconnects; read `Tester2`'s chat. 3. Stop the server and start it again, then compute the SHA-256 of the file again. 4. Cleanup: stop the server and restore the byte copy (SHA-256 read back) | Step 1: read as YAML, `gui_title` is `&6Friend List &7({COUNT}/{MAX})`, `messages.friend_online` is `&aYour friend {PLAYER} is now online!` and `messages.request_sent` is `&aFriend request sent to {PLAYER}!` — every shipped default rewritten to the English text — while `messages.friend_offline` is still `&7Bye {PLAYER}`; the recorded SHA-256 differs from the byte copy's (the file was saved). Step 2: `Tester2` receives `Your friend Tester1 is now online!` (green) — exactly the file's text — and then `Bye Tester1` (grey). Step 3: the SHA-256 equals the one recorded in step 1 (a second start with the same language writes nothing) | server | |

## Scheduled Tasks

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultisocial.task.cleanup-expired-requests | `request_timeout` set to a short, test-friendly value (e.g. 30 seconds, NOT the shipped 60-second default, purely to shorten the wait) BEFORE the request is created; `Tester1` sends `Tester3` a friend request (`ultisocial.friend.add`'s mechanism, any two players not already friends/blocked) | Wait past `request_timeout` seconds, then wait for the next 1200-tick (1-minute) sweep to run, then have `Tester3` run `/friend requests` | The request is gone from `Tester3`'s pending list without `Tester3` ever running `/friend deny` — the scheduled sweep removed it directly from the in-memory queue | server | |

## Data Persistence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultisocial.storage.friendship-and-blacklist-survive-restart | `Tester1` and `Tester2` are friends (from `ultisocial.friend.accept`); `Tester1` has blocked `Tester3` (from `ultisocial.friend.block`) | Stop the server completely, then start it again, then check BOTH `Tester1`'s and `Tester2`'s own `/friend list`, and `Tester1`'s `/friend blocklist` | `Tester1`'s `/friend list` still shows `Tester2`; `Tester2`'s OWN `/friend list` (run as `Tester2`, not `Tester1`) also still shows `Tester1` — proving the friendship is visible from BOTH accounts, not just the one that originally sent the request; `Tester1`'s `/friend blocklist` still shows `Tester3` | server | |

## Configuration

One row per shipped yml file (the config-per-file rule): `config/social.yml` (17 keys, no
packaged seed resource — generated from `SocialConfig`'s `@ConfigEntry` defaults on first boot).
This row confirms every key is present at its `FEATURES.md`-documented default, then flips one or
more representative keys and observes the behaviour follow. Three keys are removed and a fresh file
must not contain them: `notifications.friend_join_world` (`UltiKits/UltiSocial#15`),
`messages.player_blocked` and `messages.player_unblocked` (`UltiKits/UltiSocial#23`).

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultisocial.config.social-yml | `language: en` in the framework's `config.yml`; a fresh `plugins/UltiTools/pluginConfig/UltiSocial/config/social.yml`, generated by letting the module boot once with no prior file present | Load the file; confirm all 17 keys listed under `FEATURES.md`'s `## Configuration` section are present at their documented defaults (the title `gui_title` and the ten `messages.*` keys holding the English text this build writes under `language: en`, as `ultisocial.config.social.materialize-fresh` quotes), and that the file contains no `friend_join_world`, `player_blocked` or `player_unblocked` line anywhere (search the file's text for `friend_join_world`; the same search for `friend_online` must find the line under `notifications:`, which shows the search reads the file); then, in separate configurations so no toggle masks another's effect, RESTARTING THE SERVER after each edit and before observing its effect (this row verifies each value as loaded at server start; applying an edit through `/ul reload UltiSocial` without a restart is verified separately by `ultisocial.lifecycle.reload`): (a) set `max_friends: 1` (default 50), RESTART, and confirm a second friend request is refused with the max-friends message once one friendship already exists; (b) set `tp_to_friend.enabled: false` (default true), RESTART, and confirm `/friend tp` refuses as `ultisocial.friend.tp.neg-disabled` describes; (c) set `notifications.friend_online: false` (default true), RESTART, and confirm `ultisocial.event.friend-online-notify` does NOT fire; (d) set `messages.friend_added` to a distinguishable custom string (still containing `{PLAYER}`), RESTART, and confirm an `/friend accept` success message uses the new text, not the shipped default. | All 17 keys present at their documented defaults before any change; the searches for `friend_join_world`, `player_blocked` and `player_unblocked` find nothing while the control search for `friend_online` finds the `notifications:` entry (`UltiKits/UltiSocial#15` removed that key); (a) the second request is refused with `You have reached the maximum number of friends!` once the lowered limit is reached; (b) `/friend tp` refuses with `Teleport to friend feature is disabled!`; (c) no online-notification fires; (d) the accept-success message uses the custom text verbatim (with `{PLAYER}` substituted), not the shipped default | server | |

## Language

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultisocial.i18n.language | `language: en` in the framework's `plugins/UltiTools/config.yml`, set once for the session before the start; before the start, `plugins/UltiTools/pluginConfig/UltiSocial/lang/` was moved into the session backup directory so this jar's catalogues are extracted fresh (an upgraded install never refreshes an already-extracted language file, UltiKits/UltiTools-Reborn#459); `Tester1` online, holding `ultisocial.use`, with zero friends and zero blocked players | As `Tester1`, run `/friend help`, then `/friend list`, then `/friend blocklist` and read the GUI's title and its slot-22 item | `/friend help` prints the fourteen lines `ultisocial.friend.help` lists; `/friend list` prints `You have no friends yet. Use /friend add <player> to add one`; the blacklist GUI's title reads `Blacklist Management (0)` and slot 22 holds `Your blacklist is empty`. None of the text these three show contains Chinese | server | |
